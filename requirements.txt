# Core dependencies for EigenAI

# Numerical computing
# NumPy 2.0+ requires Python>=3.9, so constrain to 1.x for Python 3.8 compatibility
numpy>=1.24.0,<2.0.0

# NLP parsing (required for syntactic geometry and entropy weighting)
spacy>=3.5.0

# Optional: Visualization
# matplotlib>=3.7.0
# pandas>=2.0.0

# Testing (installed separately in CI)
# pytest>=7.3.0
